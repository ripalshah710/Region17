version: 0.2
phases:    
  install:
#    commands:
    runtime-versions:
      dotnet: 4.7
      
  build:
    commands:
      - cd src
      - dotnet build 
artifacts:
  files:
#    - '**/*'
     - appspec.yml
